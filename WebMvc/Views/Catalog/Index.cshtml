
@{
    ViewData["Title"] = "Catalog";
    @model WebMvc.ViewModels.CatalogIndexViewModel
}
<div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval="6000">
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
        <li data-target="#myCarousel" data-slide-to="3"></li>
    </ol>
    <div class="carousel-inner" role="listbox">
        <div class="item active">
            <img src="~/images/banner.jpg" alt="ASP.NET" class="img-responsive" />
            <div class="carousel-caption" role="option">
                <p>
                    Browse the fun events happeining in your city !

                </p>
            </div>
        </div>
        <div class="item">
            <img src="~/images/banner-2.jpg" alt="Visual Studio" class="img-responsive" />
            <div class="carousel-caption" role="option">
                <p>
                    Book some tickets for your romantic date night !

                </p>
            </div>
        </div>
        <div class="item">
            <img src="~/images/banner-3.jpg" alt="Package Management" class="img-responsive" />
            <div class="carousel-caption" role="option">
                <p>
                    Create event of your choice and host it for your family and friends!

                </p>
            </div>
        </div>

    </div>
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>
<section class="esh-catalog-filters">
    <div class="container">
        <form asp-controller="Catalog" asp-action="EventSearchByCategory" method="post">
            <label class="esh-catalog-label" data-title="category">
                <select asp-for="@Model.CategoryFilterApplied" asp-items="@Model.Categories" class="esh-catalog-filter"></select>
            </label>

            <label class="esh-catalog-label" data-title="type">
                <select asp-for="@Model.TypesFilterApplied" asp-items="@Model.Types" class="esh-catalog-filter"></select>
            </label>
                <input class="esh-catalog-send" type="image" src="~/images/arrow-right.svg" />

            
        </form>
        <br />
    </div>
</section>

@*<section class="esh-search-filters">
    <div class="container">
        <form asp-controller="Catalog" asp-action="EventSearchByCategory" method="post">
            <p class="esh-event-filters">
                Looking For:
                <label class="esh-catalog-label" data-title="category">

                    <select asp-for="@Model.CategoryFilterApplied" asp-items="@Model.Categories" ></select>
                </label>
                <input class="esh-catalog-send" type="image" src="~/images/arrow-right.svg" />
                </p>
        </form>

        <form asp-action="Search" asp-controller="Catalog" method="post" style="height: 100px; padding :30px">
            <p class="esh-event-filters">


                In: <input type="text" name="SearchEventCity" placeholder="Bellevue">


                <label class="esh-event-label " data-title="eventsdate">

                    On:  <select asp-for="@Model.DatesFilterApplied" asp-items="@Model.Dates" class="esh-event-filter"></select>
                </label>

            </p>
        </form>
    </div>*@
<div>
    <h1>Live Your Best Life</h1>
</div>

    <div >
        <form asp-action="Search" asp-controller="Catalog" method="post">
          
                Search Event by City :<input type="text" name="SearchEventCity" placeholder="Bellevue">
            
            <input type="submit" value="Search" />

        </form>
        <br /><br />
    </div>


<div class="container">
    @if (Model.CatalogEvents.Count() > 0)
    {
        @Html.Partial("_pagination", Model.PaginationInfo)

        <div class="esh-catalog-items row">
            @foreach (var catalogItem in Model.CatalogEvents)
            {
                <div class="esh-catalog-item col-md-4">
                    @Html.Partial("_event", catalogItem)
                </div>
            }
        </div>

        @Html.Partial("_pagination", Model.PaginationInfo)
    }
    else
    {
        <div class="esh-catalog-items row">
            THERE ARE NO RESULTS THAT MATCH YOUR SEARCH
        </div>
    }
</div>
<section class="top-categories">
    <div class="container">
        <p>
            <center>
                <h2>Categories to explore</h2>
            </center>
        </p>
    </div>
    <div class="container">
        @if (Model.Categories.Count() > 0)

        {


            <div class="esh-event row">
                @foreach (var category in Model.CatalogCategoriesWithImage)
                {

                    <div class="esh-event-item col-md-4">
                        @Html.Partial("_category", category)
                    </div>

                }
            </div>

        }

    </div>
</section>

